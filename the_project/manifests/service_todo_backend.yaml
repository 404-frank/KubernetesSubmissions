apiVersion: v1
kind: Service
metadata:
  namespace: project
  name: todo-app-backend-service
spec:
  type: ClusterIP
  selector:
    app: todo-app
  ports:
    - port: 2346
      protocol: TCP
      targetPort: 3001
